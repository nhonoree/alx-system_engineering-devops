#!/usr/bin/env bash
# This script displays all active IPv4 IP addresses attached to the machine.

# Use `ip` command to get all active IPv4 addresses and display them
ip -4 addr show | grep -oP '(?<=inet\s)\d+(\.\d+){3}'
